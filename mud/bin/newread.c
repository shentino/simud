#include <legible.h>

int main(string str) {
   string what, where = "";
   int page, fail;
   object ob;

   if( !str ) {
      notify_fail("Usage: read [page <num>|<part> of|from|on] <object>\n");
      return 0;
   }

   return 1;
}
