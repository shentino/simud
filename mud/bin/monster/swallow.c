// Swallow Whole
// this command is used by monsters
// to gulp stuff down

// dragons and purple worms use it as a combat maneuver
// ostriches use it to be a nuisance
// snakes use it to engulf their prey whole after killing it

// Targets must either be in the swallower's inventory, dead,
// or the subject of a grapple.

int main (string param)
{
   debug( "Monster called swallow command", "swallow" );
   return 1;
}
