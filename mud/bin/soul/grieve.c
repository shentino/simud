#include <emote.h>

EMOTE( "~Name ~verbgrieve over ~poss loss.@@grieving over ~poss loss",
 "~Name ~verbgrieve over the loss of ~targ.@@grieving over the loss of ~targ")

EMOTE_ATTRIBUTE( ATT_AUDIBLE | ATT_VISUAL )