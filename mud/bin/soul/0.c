#include <emote.h>

EMOTE( "~Name ~verbmake an O with ~poss hand.@@making an O with ~poss hand",
 "~Name ~verbake an O at ~targ with ~poss hand.@@making an O at ~targ with ~poss hand")

EMOTE_ATTRIBUTE( ATT_VISUAL )
