#include <emote.h>

EMOTE( "~Npos ears twitch as ~pron ~verbsay `Ahah?' for no apparent reason.@@going `ahah?'",
       "~Name ~verbgive ~targ a shifty glance, and ~pron ~verbsay `Ahah?'@@going `ahah?' at ~targ" )
