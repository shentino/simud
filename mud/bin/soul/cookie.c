#include <emote.h>

EMOTE( "~Name ~verbwant a cookie.@@pining for a cookie.",
 "~Name ~verbgive ~targ an imaginary cookie and ~verbpat ~objt on the head.@@giving ~targ a cookie and patting ~objt on the head")
