#include <emote.h>

EMOTE( "~Name ~verbgo `Dirrrr...' then ~verbtype `ls' instead.@@having a DOS relapse",
 "~Name ~verbgo `Dirrr...' at ~targ then ~verbtype `ls' instead.@@having a DOS relapse at ~targ")

EMOTE_ATTRIBUTE( ATT_FOOLISH )