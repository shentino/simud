#include <emote.h>

EMOTE( "~Name ~verblike `Foo' `cause it's metasyntactic.@@liking `Foo'",
 "~Name ~verbthink ~targ should rename ~reft to `Foo' because ~prot would be metasyntactic, then.@@fooing ~targ")

EMOTE_ATTRIBUTE( ATT_FOOLISH )