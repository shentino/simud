#include <emote.h>

EMOTE( "~Name ~verbdeclare that ~pron ~verbhas returned.@@declaring that ~pron ~verbhas returned",
 "~Name ~verbdeclare that ~targ ~verthas returned.@@declaring that ~targ ~verthas returned")

EMOTE_ATTRIBUTE( ATT_AUDIBLE )