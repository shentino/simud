#include <emote.h>

EMOTE( "~Name ~verbmake an `X' with ~poss fingers.@@making an `X' with ~poss fingers",
 "~Name ~verbmake an `X' at ~targ with ~poss fingers.@@making an `X' at ~targ with ~poss fingers")
