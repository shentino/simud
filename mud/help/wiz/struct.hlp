WIZARD HELP
STRUCTS
As of LDMud 3.3.246, LPC supports structs.  Previously, if a struct was needed, a string-indexed mapping was used instead.  This will still work, of course.

See: ~CREFstructs~CHLP (LDMud documentation)
