WIZARD HELP
SETOWNER
Administrators only. Takes a file/directory and a UID as a parameter. This function
changes the file/directory ownership to the UID. Normal wizards who wish to
use different file ownerships should create their file in a directory where
the ownership matches the UID they need.

See also: ~CREFsecurity, uid, owner, groups