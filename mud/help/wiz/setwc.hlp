WIZARD HELP
SETWC (SET WEAPON CLASS)
Takes three parameters -- a player or creature (who must be present when you
use the command), a WC type, and a value. The WC types (the same as the AC
types) are listed in /include/battle.h. If you give 'all' as the WC type, it sets all the values
at once.

See also: ~CREFsetac