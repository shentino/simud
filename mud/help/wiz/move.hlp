WIZARD HELP
MOVE
Takes two object references as parameters. Moves the first object inside the
second object. Useful for transporting things, but confusing if used on
players because they won't realize that they've been moved. Use trans instead.

See also: ~CREFgoto~CHLP, ~CREFtrans~CHLP, ~CREFreference~CHLP
