WIZARD HELP
CAT
Takes a file as a parameter and dumps it to your terminal. Handy for short
files and (with -l) taking a peek at code where a bug is occuring. Use
~CCOMmore~CHLP or the web editor for longer files.

Options:
 ~CCOM-p~CHLP: process color codes (~CCOMcat -p /text/xpanse.cod~CHLP)
 ~CCOM-n~CHLP: print line numbers
 ~CCOM-l start end~CHLP: print lines ~CCOMstart~CHLP to ~CCOMend~CHLP

See also: ~CREFmore
