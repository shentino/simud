WIZARD HELP
STAT
The ~CCOMstat~CHLP command displays information on an object that is
saveload capable. Essentially, it saves the object to disk, then loads
the file from disk and formats the contents into something readable.
It only takes one parameter -- the object reference.

See also: ~CREFreference