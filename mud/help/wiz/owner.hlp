WIZARD HELP
OWNER
Takes a file or a directory as a parameter. This command tells you who the
owner of that file or directory is. If the file has a common extension, such
as .c or .o, you need not specify it.

See also: ~CREFuid, setowner, groups