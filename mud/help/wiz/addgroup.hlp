WIZARD HELP
ADDGROUP
Takes a UID and a group as a parameter. Grants the UID access to the group
you specify, if you have rights to control that group. Wizards control only
their own (primary) UID, Archwizards have control over all the UID's they've
been granted access too.

See also: ~CREFuid, groups, rmgroup