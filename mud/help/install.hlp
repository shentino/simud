PLAYER COMMANDS
INSTALL
This command is used to install something (typically a lock) in something else (typically a door). The command is usually just ~CCOMinstall lock in door~CHLP.

You can only install a lock if there are no other locks installed.

~CCOMSee also: ~CREFuninstall
