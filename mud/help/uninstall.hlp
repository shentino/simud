PLAYER COMMANDS
UNINSTALL
This command is used to uninstall something (typically a lock) from something else (typically a door). The command is usually just ~CCOMuninstall lock from door~CHLP.

You can only uninstall a lock if you are carrying the matching key.

~CCOMSee also: ~CREFinstall
