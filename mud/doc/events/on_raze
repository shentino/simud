int on_raze()

Called whenever an object is razed. This should destruct the object (if
necessary), print any messages, and return a non-zero value. If you don't
return a non-zero value, a generic error will be printed.
